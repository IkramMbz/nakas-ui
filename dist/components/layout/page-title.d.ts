import { ElementSize } from "src/types.js";
type PageTitleProps = {
    title: string;
    subtitle?: string;
    size?: ElementSize;
};
declare const PageTitle: ({ title, subtitle, size, }: PageTitleProps) => React.ReactElement;
export default PageTitle;
//# sourceMappingURL=page-title.d.ts.map